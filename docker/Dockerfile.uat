FROM alpine:3.19
RUN echo "Hello from Coolify Docker build test"
RUN apk add --no-cache curl wget
EXPOSE 3001 3010
HEALTHCHECK CMD wget --no-verbose --tries=1 --spider http://localhost:3001/ || exit 1
CMD ["sleep", "infinity"]
