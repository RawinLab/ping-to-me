{
  "name": "redirector",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env development",
    "start": "wrangler dev --env development",
    "deploy": "wrangler deploy --env production",
    "deploy:uat": "wrangler deploy --env uat",
    "deploy:prod": "wrangler deploy --env production",
    "preview": "wrangler deploy --env development --dry-run"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.1"
  }
}
