# Archived E2E Tests

These tests are legacy debugging/exploration artifacts from early development.
They have been archived in favor of the new structured test suite.

New tests are in subdirectories: auth/, links/, analytics/, etc.

To run archived tests (not recommended):
  npx playwright test e2e/_archive/some-file.spec.ts
